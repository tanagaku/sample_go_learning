name: proto-gen

on:
  workflow_run:
    workflows: ["CI"]
    types: [completed]

jobs:
  protoc:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
      - name: test
        run: echo "TEST RUN"
